{
  "name": "mcp-defaults",
  "description": "Always-on MCP servers (context7, deepwiki, ck, codegraph) so every project gets them without a per-repository .mcp.json",
  "version": "1.1.0",
  "author": {
    "name": "Xantibody"
  }
}
