{
  git = {
    enable = true;
  };
  starship = {
    enable = true;
  };
  fish = {
    enable = true;
  };
  hyprland = {
    enable = true;
  };
  # Install firefox.
  firefox = {
    enable = true;
  };
  ssh = {
    startAgent = true;
  };
}
